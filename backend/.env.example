# --- Airtable Configuration ---
# Find these values in your Airtable account and API documentation for your base.
AIRTABLE_API_KEY=your_personal_access_token_from_airtable
AIRTABLE_BASE_ID=your_base_id_starting_with_app

# --- Airtable Table Names ---
# These must exactly match the names of the tables in your Airtable base.
PROJECTS_TABLE=Projects
CLIENTS_TABLE=Clients
DELIVERABLES_TABLE=Deliverables

# --- Application Admin Credentials ---
# The default username and password for the admin account.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password

# --- Server Configuration ---
# The port the backend Node.js server will run on inside its container.
# This should match the port used in the backend service and nginx proxy config.
PORT=3001